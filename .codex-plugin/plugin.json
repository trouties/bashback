{
  "name": "bashback",
  "version": "1.0.1",
  "description": "Undo for the shell commands your coding agent runs — snapshots the working tree around every command so file side effects (rm, mv, sed -i, ...) can be audited and restored.",
  "author": { "name": "trouties" },
  "license": "AGPL-3.0-or-later",
  "keywords": ["undo", "snapshot", "checkpoint", "rewind", "shell", "agent"],
  "skills": ["./skills"]
}
